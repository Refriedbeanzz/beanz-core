## [2026-04-10] Ability Input Is Not Global

### Finding
Hytale does not treat Ability1/2/3 as global key events.

### Details
- Ability inputs are resolved through item interaction mappings
- If no interaction exists on held item:
  - no packet is sent
  - no server event fires
- Implication:
  - Packet watchers alone cannot detect Ability3
  - Interaction must exist first on client-side assets

---

## [2026-04-10] PacketWatcher Limitation

### Finding
Inbound packet watchers only work if interaction packets are generated by client.

### Details
- No `SyncInteractionChains` packets observed without valid interaction
- Confirms:
  - server cannot detect unused ability keys
  - input must be bound to an interaction before it exists in network flow

---

## [2026-04-10] Asset Injection Risk

### Finding
Injecting interactions into all item assets can break asset validation.

### Details
- Global injection caused mod load failure
- Likely causes:
  - invalid root interaction references
  - corrupted asset maps
- Safer approach:
  - inject into a single controlled asset first
---

## [2026-04-10] Dedicated Ability3 Item Path

### Finding
The safest first proof for Ability3 is a dedicated custom item that explicitly maps `Ability3` in its own item asset.

### Details
- Avoids runtime mutation of loaded assets
- Limits validation scope to:
  - one item asset
  - one root interaction asset
  - one Java interaction codec
- Makes Ability3 testing depend only on holding the custom item
